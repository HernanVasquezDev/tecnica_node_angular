// This file defines the structure of a Usuario object in TypeScript. It includes properties for id, nombre, correo, and edad, all of which are required and have specific types.
export interface Usuario {
    id: number;
    nombre: string;
    correo: string;
    edad: number;
}
